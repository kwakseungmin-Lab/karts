# karts — Core Dependencies (ported from OpenMontage)
# Paid API SDKs (fal-client, elevenlabs, suno-api, etc.) have been removed.
# Open-source / free alternatives are listed below.

# --- Core ---
pyyaml>=6.0
pydantic>=2.0
jsonschema>=4.20
python-dotenv>=1.0
Pillow>=10.0
requests>=2.31

# --- Local video generation (open-source) ---
# LTX-Video, Wan2.1, CogVideoX via diffusers
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0

# --- Local image generation (open-source) ---
# Stable Diffusion / FLUX.1 via diffusers (same deps as above)
# FLUX.1-dev and FLUX.1-schnell weights available on HuggingFace

# --- GPU (optional, install separately when GPU is available) ---
# torch>=2.0
# torchaudio>=2.0
# torchvision>=0.15

# --- Local TTS (open-source) ---
# Piper TTS — install the binary: https://github.com/rhasspy/piper/releases
# No Python package needed; piper_tts.py calls the CLI binary.

# --- Free music / audio (no API key required for CC0 Freesound previews) ---
# freesound_music.py uses requests + FREESOUND_API_KEY (free tier)
# music_gen.py and pixabay_music.py also available as fallbacks

# --- Optional: video processing ---
moviepy>=1.0.3
ffmpeg-python>=0.2.0
